<Window x:Class="FloorballDJ.Views.DeckFadeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Fade för deck" Width="470" Height="330" ResizeMode="NoResize" WindowStartupLocation="CenterOwner">
    <Grid Margin="24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Fade in och fade ut" Style="{StaticResource SectionTitle}" FontSize="23" />
        <TextBlock x:Name="DeckNameText" Grid.Row="1" Foreground="{StaticResource MutedBrush}" Margin="0,4,0,8" />
        <TextBlock Grid.Row="2" Text="Värdet appliceras på alla jinglar i decket. Lämna en ruta tom för att använda den globala inställningen."
                   TextWrapping="Wrap" Foreground="{StaticResource MutedBrush}" Margin="0,0,0,18" />
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="16" /><ColumnDefinition Width="*" /></Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="FADE IN (SEKUNDER)" Style="{StaticResource FieldLabel}" Margin="0,0,0,5" />
                <TextBox x:Name="FadeInBox" TextAlignment="Center" />
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock Text="FADE UT (SEKUNDER)" Style="{StaticResource FieldLabel}" Margin="0,0,0,5" />
                <TextBox x:Name="FadeOutBox" TextAlignment="Center" />
            </StackPanel>
        </Grid>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Avbryt" Style="{StaticResource SecondaryButton}" MinWidth="96" Margin="0,0,8,0" IsCancel="True" />
            <Button Content="Använd på deck" Style="{StaticResource PrimaryButton}" MinWidth="132" IsDefault="True" Click="Save_Click" />
        </StackPanel>
    </Grid>
</Window>
